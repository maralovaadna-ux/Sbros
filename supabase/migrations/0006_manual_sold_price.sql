-- =========================================================
-- Миграция 0006: цена для ручного штампа "Продано"
-- =========================================================

alter table public.offers
  add column manual_sold_price numeric(12,2);

comment on column public.offers.manual_sold_price is 'Вручную выставленная цена для штампа "Продано X/Y по Z ₸" — не связана с реальными уровнями цены, только для фейковых/завершённых демо-предложений';
